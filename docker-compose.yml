services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: meetbot
      POSTGRES_USER: meetbot
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: